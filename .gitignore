# Local config — the whole point is that these are per-machine.
# Only the *.example files belong in git.
config/*
!config/*.example

# Runtime artefacts
*.log
binary-shas.txt
toolchain-sbom-*.json

# Local agent guardrails — maintainer context, not project documentation.
# These name the employer-specific identifiers that must never reach a public
# repo, so they are exactly the files that must not reach one themselves.
CLAUDE.md
CLAUDE.local.md
AGENTS.md
GEMINI.md
.kiro/
.cursor/
.windsurfrules
.aider*

.DS_Store
